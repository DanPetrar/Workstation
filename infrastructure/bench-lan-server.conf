# Serve NTP to the bench-LAN development range (192.168.20.64-.255), added 2026-08-06.
# Local backup time source for ZaxModbus units and dev machines on this range —
# pool.ntp.org is unreliable on the bench LAN even though the Workstation's own
# upstream sync is fine. See Workspace/nodes/workstation/status.md.
allow 192.168.20.64/26
allow 192.168.20.128/25
